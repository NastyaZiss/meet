//экран для создание организаторами мероприятия. Должен быть доступен только организатору.

import 'package:flutter/cupertino.dart';

class CreatScreen extends StatelessWidget {
  const CreatScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
